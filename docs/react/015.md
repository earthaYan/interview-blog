# react 服务端渲染-数据库相关

切换到 vercel 该项目下的 storage 选项卡，使用`Neon`创建数据库,创建完成后切换到`.env.local`,将其代码复制到本地的 env 文件内。

[navi Cat 报错原因](https://stackoverflow.com/questions/78645635/how-to-connect-postgresql-vercel-in-navicat)

## 数据库操作

```js
import postgres from "postgres";

const sql = postgres(process.env.POSTGRES_URL!, { ssl: "require" });

//执行语句
sql`SQL语句`
```
